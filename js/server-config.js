// Address of the NoteQuest account server (server/ folder, deployed on Railway).
// Set it to your Railway domain to turn on accounts that work on every device, e.g.
//   window.API_BASE = 'https://notequest-production.up.railway.app';
// Leave it empty to keep device-only accounts.
window.API_BASE = '';
